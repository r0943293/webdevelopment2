const setup = () => {

    let tekst = "Gisteren zat de jongen op de stoep en at de helft van de appel";

    let input;
    for (let i = 1; i < tekst.length; i++) {
        input = (tekst[i] + tekst[i + 1]).toString();
        if (input === "de") {

        }

    }



}

window.addEventListener("load", setup);